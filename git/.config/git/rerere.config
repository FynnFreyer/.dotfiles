# rerere -> reuse recorded resolution
# remember how you’ve resolved conflict so next time, Git can resolve it automatically
# https://www.git-scm.com/book/en/v2/Git-Tools-Rerere
[rerere]
    enable=true
[gc]
   rerereUnresolved=1095
   rerereResolved=1095

