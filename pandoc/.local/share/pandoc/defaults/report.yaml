defaults:
  - base.yaml
  - admonitions.yaml  # defines fenced div admonition blocks

metadata-files:
  - ${.}/../style/yaml/dense.yaml
  - ${.}/../style/yaml/no_title.yaml

variables:
  pagestyle: empty
  title: null

