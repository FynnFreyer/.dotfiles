defaults:
  - base.yaml
  - pageenvs.yaml  # defines environments for use via fenced divs

metadata-files:
  - ${.}/../style/yaml/dense.yaml
  - ${.}/../style/yaml/no_title.yaml
  - ${.}/../style/yaml/toc_lite.yaml

variables:
  pagestyle: empty

